import { FiltroDiplomadoPipe } from './filtro-diplomado.pipe';

describe('FiltroDiplomadoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDiplomadoPipe();
    expect(pipe).toBeTruthy();
  });
});

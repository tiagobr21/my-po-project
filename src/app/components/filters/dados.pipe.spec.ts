import { DadosAlunosPipe } from './dadosAlunos.pipe';

describe('DadosAlunosPipe', () => {
  it('create an instance', () => {
    const pipe = new DadosAlunosPipe();
    expect(pipe).toBeTruthy();
  });
});
